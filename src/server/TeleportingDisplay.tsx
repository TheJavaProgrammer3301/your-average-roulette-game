import React from "@rbxts/react";

export default function TeleportingDisplay() {
	return <textlabel
		Size={UDim2.fromScale(1, 1)}
		BackgroundTransparency={1}
		Text="Teleport in progress..."
		FontFace={Font.fromId(12187365364)}
		TextColor3={Color3.fromRGB(12, 207, 135)}
		TextSize={72}
		TextWrapped
	/>
}